import express from 'express';

const router = express.Router();

// TODO: Add specific upload-related routes here
// Example: router.post('/tus-complete', tusCompleteController);

export default router;
