# GitHub Container Registry Organization
ORGANIZATION=johnr24

# Image tag to use (latest, v1.0.0, etc.)
TAG=latest

# Domain configuration
DOMAIN=live.colourstream.example.com
VIDEO_DOMAIN=video.colourstream.example.com
ADMIN_EMAIL=admin@example.com

# Database configuration
DB_USER=colourstream
DB_PASSWORD=change_this_to_secure_password
DB_NAME=colourstream

# Security keys - generate secure random strings for these
JWT_KEY=change_this_to_secure_random_string
ADMIN_AUTH_SECRET=change_this_to_secure_random_string 