# S3 bucket name
S3_BUCKET=uploads

# S3 region
S3_REGION=us-east-1

# S3 credentials (Option 1)
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key

# S3 credentials (Option 2, if not using the above)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# S3 endpoint (for MinIO or other S3-compatible storage)
# Use full URL including protocol (http:// or https://)
S3_PUBLIC_ENDPOINT=https://s3.colourstream.johnrogerscolour.co.uk

# Mount point inside container
S3_MOUNT_POINT=/mnt/s3fs

# Architecture of your Mac (arm64 for Apple Silicon, amd64 for Intel)
ARCH=arm64

# Additional mount options (space-separated, refer to mount-s3 documentation)
# Example: "--allow-other --allow-delete"
MOUNT_OPTIONS= 