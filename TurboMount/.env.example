# S3 bucket name to mount
S3_BUCKET=your-bucket-name

# AWS credentials
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# Region where your bucket is located (default: us-east-1)
S3_REGION=us-east-1

# For MinIO or other S3-compatible storage, specify the endpoint URL
# Example for local MinIO: http://minio:9000
# Leave empty for AWS S3
S3_ENDPOINT=

# Architecture of your Mac (arm64 for Apple Silicon, amd64 for Intel)
ARCH=arm64

# Additional mount options (space-separated, refer to mount-s3 documentation)
# Example: "--allow-other --allow-delete"
MOUNT_OPTIONS= 